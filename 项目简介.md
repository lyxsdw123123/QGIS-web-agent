# QGIS Web Agent（复现版）

> **原作声明（Attribution）**
> 本项目是对以下原作的复现，仅用于学习与技术研究，版权归原作者所有。
>
> - 原作名称：**QGIS Web Agent · 对话式地理处理**
> - 原作者：**张翼**（Wing's Space）
> - 原作地址：<https://wing-show.com/projects/qgis-web-agent/>
>
> 原作的核心思路与本项目一脉相承：**在浏览器里用自然语言调用服务器端 QGIS 做地理处理，
> 结果叠加在天地图上并可下载复查**。本项目在保留这一核心链路的基础上，做了极简化与重构。

---

## 这是什么

一个「对话式 WebGIS 智能体」：用户在网页对话框里用自然语言提出地理处理需求，
大模型理解意图并选择工具，**真正的几何计算交给服务器端的 QGIS 引擎执行**，
结果图层叠加在地图上，可下载 GeoJSON / PNG 复查。

它要解决的核心问题：**不让大模型直接"写 GIS 代码"**——因为那样结果不可靠、无法落地。
而是把常见 GIS 操作封装成固定工具，大模型只当"调度员"，计算永远是确定性、可复现的。

## 能做什么

共实现 **17 个 GIS 工具**，分四类：

| 类别 | 工具 | 说明 |
|---|---|---|
| 矢量分析 | 缓冲区、沿线采样、裁剪、按属性筛选、重投影 | 由 QGIS `native:*` 算法执行 |
| 统计检查 | 图层检查、字段统计、长度统计、转 GeoJSON | 纯 Python，直接读 GeoJSON |
| 数据获取 | OSM 道路、行政区划边界、DEM 高程 | 公开数据源（OSM / Nominatim / AWS 地形瓦片） |
| 地形与出图 | 坡度、坡向、山体阴影、渲染 PNG | QGIS 栅格算法 + GDAL |

典型对话示例：

- 「加载示例道路，看看有哪些字段，筛选主干道并统计长度」
- 「获取武汉市江汉区的 DEM，计算坡度和山体阴影」
- 「下载武汉市青山区的 OSM 道路，做 200 米缓冲」

## 怎么实现的

三层分工：

```
浏览器（Leaflet + OSM 底图 + 聊天 + 图层面板）
   │  fetch
   ▼
FastAPI 后端（会话、图层、上传、静态服务）
   │
   └─ 大模型（通义千问 function calling）── 只负责「选工具 + 填参数」
        │
        ▼
     17 个工具 ── 真正的计算交给：
        ├─ qgis_process（QGIS 命令行，native:buffer 等）
        ├─ GDAL / rasterio（栅格转换、DEM 解码）
        └─ 公开数据源（Overpass / Nominatim / AWS Terrain Tiles）
        │
        ▼
     结果 GeoJSON / PNG → 回传前端上图、可下载
```

技术栈：Leaflet · FastAPI · 通义千问 Qwen（function calling）· QGIS 3.x（qgis_process）·
GeoPandas / rasterio / NumPy / Pillow。

## 怎么运行

```powershell
cd qgis-web-agent-mvp
python -m uvicorn backend.main:app --reload --port 8000
# 打开 http://127.0.0.1:8000
```

前置条件：安装 QGIS（配置 `QGIS_PROCESS_PATH`）、配置通义千问 `DASHSCOPE_API_KEY`
（见 `.env`，未提交）。

## 与原作的关系

| 方面 | 原作 | 本复现版 |
|---|---|---|
| 核心链路 | 自然语言 → QGIS → 地图 | 一致 |
| 工具数量 | 17 个 | 17 个（对齐） |
| 底图 | 天地图 / 百度（火星坐标纠偏） | OSM（免 key，无纠偏） |
| 工具协议 | MCP | 直接 function calling（单入口无需 MCP） |
| 流式进度 | SSE 流式 + 工具卡片 | SSE 流式 + 工具卡片（一致） |
| 框选 / 多文件上传 / 会话管理 | 有 | 未做（简化） |

项目源码见：<https://github.com/lyxsdw123123/QGIS-web-agent>
